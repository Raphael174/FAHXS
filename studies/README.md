# Studies

Historical parametric studies and exploratory sweeps live here instead of in the package root.

These scripts may need small import-path updates before rerunning because they were moved out of `1Dmodel/`. The production user entry points are:

```powershell
python -m hps_combustor.main_steady
python -m hps_combustor.main_transient
```
