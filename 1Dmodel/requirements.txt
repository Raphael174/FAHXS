# Core scientific stack
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# Thermodynamic properties (coolant side — Helium real-gas EOS)
CoolProp>=6.4

# Combustion chemistry (hot-gas side — Cantera 3.x recommended)
# Install via conda:  conda install -c cantera cantera
# Install via pip:    pip install cantera
cantera>=3.0
